

函数输入模板：

    所有函数在输入的时候，
    都要保证最外层被一对 "()" 包括，
    不然会直接判定输入错误

    正确：(* a b)
        表示 a*b
    错误：* a b
        表达式没有被()包裹，判定错误

基本表达式输入格式：
    赋值：(:= a 1) -> a=1
    加法：(+ a 2) -> a=3
    赋值：(:= b=2) -> b=2
    乘法：(* a b) -> a*b=6
    复合公式：
    (+())